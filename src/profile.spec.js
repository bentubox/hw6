const expect = require('chai').expect
const fetch = require('isomorphic-fetch')

const url = path => `http://localhost:3000${path}`

describe('Test of backend headline functionality for user profiles', ()=> {
    xit('should retrieve the headline for the default user', (done) => {
        
    })

    xit('should retrieve the proper headline from memory for a specified user', (done) => {
        
    })

    xit('should update the headline in memory for the default user', (done) => {
        
    })
})